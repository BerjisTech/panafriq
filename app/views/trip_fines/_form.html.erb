<%= form_with(model: trip_fine) do |form| %>
  <% if trip_fine.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trip_fine.errors.count, "error") %> prohibited this trip_fine from being saved:</h2>

      <ul>
        <% trip_fine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :trip_id, style: "display: block" %>
    <%= form.text_field :trip_id %>
  </div>

  <div>
    <%= form.label :fine, style: "display: block" %>
    <%= form.text_area :fine %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
