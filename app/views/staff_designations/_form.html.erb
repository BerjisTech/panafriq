<%= form_with(model: staff_designation) do |form| %>
  <% if staff_designation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(staff_designation.errors.count, "error") %> prohibited this staff_designation from being saved:</h2>

      <ul>
        <% staff_designation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
