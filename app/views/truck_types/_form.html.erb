<%= form_with(model: truck_type) do |form| %>
  <% if truck_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(truck_type.errors.count, "error") %> prohibited this truck_type from being saved:</h2>

      <ul>
        <% truck_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_area :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
